{% extends 'base.html.twig' %}

{% block title %}Hello Event{% endblock %}

{% block body %}
    <h1>{{ event.name }}</h1>

    <hr>

    <div class="row">
        <div class="col-6">
            <img src="{{ event.image }}" alt="Une image lorem picsum aléatoire">
        </div>
        <div class="col-6">
            <p>{{ event.description }}</p>
        </div>
    </div>
{% endblock %}